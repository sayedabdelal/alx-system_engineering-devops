#!/usr/bin/env bash
# Displays the time for 12 horus and 59 minutes.

hour=0

while [ $hour -le 12 ]
do
    min=1
    echo "hour: $hour"
    while [ $min -le 59 ]
    do
        echo $min
        (( min++ ))
    done
    (( hour++ ))
done
